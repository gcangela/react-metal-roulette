import React from 'react'
import bootstrap from 'bootstrap'
import RandomBand from './RandomBand';

const BandSearch = ({randomBandRequest, randomBand}) => {
  console.log('in bandsearch',randomBand)
  return(
    <div>
      
    </div>
  )
}
export default BandSearch